<form method="post" action="{{ path('fond_delete', {'id': fond.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fond.id) }}">
    <button class="btn btn-lg btn-primary">Delete</button>
</form>
